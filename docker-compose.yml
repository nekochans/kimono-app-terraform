version: '3.7'
services:
  terraform:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - TERRAFORM_VERSION=0.13.0
    tty: true
    volumes:
      - .:/data
      - $HOME/.aws:/root/.aws
